import React, {useState} from 'react'

export const Todo = ({todo, liftTodoHandler}) => {
  /*const [todoTitle, setTodoTitle] = useState('');
  const changeHandler = ({target}) => {
    setTodoTitle(target.value)
  }
  
  const addTodoHandler = ({target}) => {
   // console.log(target.closest('.todo-list__item').children[1].value)
    const newTodo = {
      id: +(new Date()),
      title: target.closest('.todo-list__item').children[1].value
    }
    liftTodoHandler(newTodo);
    setTodoTitle('');
  }*/
  return (
    <>
      {/*<p>{todo}</p>*/}
      
    </>
  )
}
